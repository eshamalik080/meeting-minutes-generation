// Mirrors backend/app/config.py — keep these in sync if that file changes.
export const ALLOWED_EXTENSIONS = [".mp3", ".mp4", ".wav", ".m4a", ".webm", ".mov", ".flac", ".ogg"];
export const MAX_UPLOAD_MB = 500;
