venv/
__pycache__/
*.pyc
.env

# Uploaded media and generated exports — never commit user data.
storage/uploads/*
storage/outputs/*
!storage/uploads/.gitkeep
!storage/outputs/.gitkeep

# Local auth database (user emails + password hashes) — never commit.
storage/*.db
